Place logo template images here as tight crops of the on-screen logos used by each detection profile.

Current profile assets:

- `google_logo_template.png` for the `google_search` profile
- `logo_template.png` for the fallback `generic_blue_anchor` profile

The Python service uses these images for OpenCV template matching inside each profile's indicator region.
